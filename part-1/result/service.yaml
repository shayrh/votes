#part1 - service/deployment.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-voting-app-result
  namespace: voting-app
  #use namespace voting-app
spec:
  selector:
    app: my-voting-app-result
  ports:
    - protocol: TCP 
      nodePort: 31001
      port: 80
      targetPort: 80  
  type: NodePort


